@model IEnumerable<Book_Store.Models.Role>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "UpdateRoleById", new { id = item.Role1 }) |
            @Html.ActionLink("Details", "GetRoleById", new { id=item.Role1 }) |
            @Html.ActionLink("Delete", "DeleteRoleById", new { id = item.Role1 })
        </td>
    </tr>
}

</table>
